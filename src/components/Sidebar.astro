<aside class="sidebar">
  <div class="sidebar__brand">
    <a href="/">
      <h1 class="sidebar__title">Christopher<br/>Olberding</h1>
    </a>
  </div>

  <div class="sidebar__content">
    <p class="sidebar__bio">I write about technology, design, and the ideas that shape how we live and work. This site is a place for thinking out loud — exploring questions that don't have easy answers. When I'm not writing, I'm usually reading widely across fields or building things on the web.</p>
    <a href="/about" class="sidebar__link">Learn more about me &rarr;</a>
  </div>
</aside>

<style>
  .sidebar {
    background-color: var(--color-sidebar);
    color: var(--color-text);
    padding: var(--spacing-md);
    display: flex;
    flex-direction: column;
    min-height: 100%;
  }

  .sidebar__brand {
    padding-top: 0;
  }

  .sidebar__brand a {
    text-decoration: none;
    color: inherit;
  }

  .sidebar__title {
    font-family: 'Outfit', var(--font-sans);
    font-size: 3rem;
    font-weight: 800;
    line-height: 0.8;
    letter-spacing: -0.053em;
    color: var(--color-text);
    text-align: right;
  }

  .sidebar__content {
    margin-top: 2rem;
    display: flex;
    flex-direction: column;
    align-items: flex-end;
  }

  .sidebar__bio {
    font-family: 'Literata', var(--font-serif);
    font-size: 0.9rem;
    line-height: 1.6;
    margin-bottom: var(--spacing-md);
  }

  .sidebar__link {
    display: inline-block;
    font-size: 0.85rem;
    font-weight: 600;
    text-decoration: none;
    color: var(--color-text);
    background-color: rgba(255, 255, 255, 0.3);
    padding: 0.5em 1em;
    transition: opacity 0.2s;
  }

  .sidebar__link:hover {
    opacity: 0.85;
  }
</style>
