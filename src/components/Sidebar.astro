<aside class="sidebar">
  <div class="sidebar__brand">
    <a href="/">
      <h1 class="sidebar__title">Christopher<br/>Olberding</h1>
    </a>
  </div>

  <div class="sidebar__content">
    <p class="sidebar__bio">I've worked across brand, technology, product, and performance marketing. Not as adjacent specialties but as a connected system. That's made me useful in rooms where most only understand part of the picture and honest about something the profession avoids: most of what we call data-driven doesn't survive serious scrutiny.</p>
    <p class="sidebar__bio">I've built and sold an agency, managed eight-figure budgets, and led acquisition for one of the largest banks in the country. I write about where the borders of marketing knowledge actually are, and what it means to lead inside them.</p>
    <a href="/about" class="sidebar__link">Learn more about me &rarr;</a>
  </div>
</aside>

<style>
  .sidebar {
    background-color: var(--color-sidebar);
    color: var(--color-text);
    padding: var(--spacing-md);
    display: flex;
    flex-direction: column;
    min-height: 100%;
  }

  .sidebar__brand {
    padding-top: 0;
    display: flex;
    justify-content: flex-end;
  }

  .sidebar__brand a {
    text-decoration: none;
    color: inherit;
  }

  .sidebar__title {
    font-family: 'Outfit', var(--font-sans);
    font-size: 3rem;
    font-weight: 800;
    line-height: 0.8;
    letter-spacing: -0.053em;
    color: var(--color-text);
    text-align: right;
  }

  .sidebar__content {
    margin-top: 2rem;
    display: flex;
    flex-direction: column;
    align-items: flex-end;
  }

  .sidebar__bio {
    font-family: 'Literata', var(--font-serif);
    font-size: 0.9rem;
    line-height: 1.6;
    margin-bottom: var(--spacing-md);
  }

  .sidebar__link {
    display: inline-block;
    font-size: 0.85rem;
    font-weight: 600;
    text-decoration: none;
    color: var(--color-text);
    background-color: rgba(255, 255, 255, 0.3);
    padding: 0.5em 1em;
    transition: opacity 0.2s;
  }

  .sidebar__link:hover {
    opacity: 0.85;
  }
</style>
